---
tags: [daily]
created: 2026-06-02
---

# 2026-06-02

## 🎯 Focus
- Stand up the project knowledge base in Obsidian.

## 🛠️ Worked On
- Created the in-repo Obsidian vault at `docs/` (reused existing Obsidian config).
- Wrote core notes: [[Project Overview]], [[Architecture]], [[Voice Pipeline]], [[LLM & ASR Providers]], [[Tech Stack]], [[Settings & Security]], [[Tasks]], [[Decisions]].
- Captured 3 ADRs and a security follow-up checklist.
- 🐞 Fixed a machine-freezing crash when using Hue during a Google Meet call. Root cause: in companion mode Whisper still loaded fp32 on **WebGPU**, contending with the call's video pipeline → VRAM exhaustion / driver TDR. Now pinned to wasm/CPU in companion mode (Kokoro was already skipped there); the worker disposes any resident GPU model on switch. New [[Decisions|ADR-004]]. Touched `whisper.worker.ts`, `transcription.ts`, `useVoiceMode.ts`.

## 🐛 Issues / Blockers
- Old stale vault still registered at `C:\dev-proj\hue-desktop\docs` — can be removed via Obsidian *Manage vaults*.

## 📝 Notes
- Per-machine Obsidian UI state (`workspace.json`, `workspace-mobile.json`) is now gitignored; the rest of `.obsidian/` (plugins, templates, graph) is tracked.

## ➡️ Next
- Pick off a security task from [[Tasks]] (e.g. re-evaluate `sandbox: false`).
- Document the resume-ingestion flow.

Back to [[Daily Notes]] · [[Home]].
